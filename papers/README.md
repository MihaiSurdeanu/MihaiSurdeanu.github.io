Place publication PDFs here. Use descriptive filenames such as `short-paper-title-2026.pdf`.

In each publication entry, replace the PDF placeholder span with a link:

```html
<a href="papers/short-paper-title-2026.pdf">PDF</a>
```

Replace the code placeholder with the actual repository link, or remove it if no code is available.
