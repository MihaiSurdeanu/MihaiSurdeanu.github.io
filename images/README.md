Place your portrait here, for example `mihai.jpg`.

In `index.html`, change the portrait's `src` to `images/mihai.jpg` and its `alt` to `Mihai Surdeanu`. The included SVG is a labeled placeholder, not an actual portrait. You may delete it after replacing it.
